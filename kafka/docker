version : '3.2'

services:
  db:
    image: mysql
    mem_limit: 500m
    ports:
      - "3306:3306"
    networks:
      - im-docker-network
    environment:
      MYSQL_ROOT_PASSWORD: admin

networks:
  im-docker-network: